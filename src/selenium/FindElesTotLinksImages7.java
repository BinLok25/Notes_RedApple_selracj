package selenium;

public class FindElesTotLinksImages7 {
	/**
	 * 1.total links
	 * 2.print the text of each link
	 * 3.avoid blank text
	 * 4.broken link-later
	 * 
	 * use the website freshworks.com
	 * 
	 * find out all the elements-->FEs-returns collection/list of all eles on specific
	 * page
	 * List - is parent interface of ArrayList and is order based
	 * FEs - says give me the By locator
	 * 
	 * 
	 * broken link: u click on the link, u get 404/504 error
	 * blank link: the text is null/nothing=>size ==0
	 * 
	 * common thing for all links-is anchor tag <a>
	 * 
	 * WEB-SCRAPPING:
	 * =================>>
	 * Eles are dynamic in nature -- the no of eles not constant all the time
	 * can print the alt values and src values of diff websites
	 * ---------fetching the data of different applications and store it in excel file
	 * and give it to the product team of another web app -- with prices to compare 
	 * and reduce their prices on their own websites to adjust the price and attract
	 * the crowds
	 * 
	 * 
	 * 
	 */

}

package faqs102;

public class FinallyFinalFinalizeConcepts10 {
	
	/**
	 * final:i) a keyword to stop the var from being assigned a new value
	 *   //to declare constant values
	 * 		ii)can also use in inheritance concept=> declare the parent class final and
	 * 		prevent inheritance 
	 * 
	 *  //to prevent inheritance
	 *	//to prevent method overriding
	 *
	 * finally: is a block which gets executed whether exception is coming or not
	 *
	 * usecases: to close the data base used to close connectivity inside the finally
	 * block
	 * 
	 * finalize: is a method
	 * //used to perform the clean up -- System.gc(); --if u declared finalize() method
	 * before System.gc();
	 * to clean up before the garbage collector comes in - cleans up the
	 * objs with no references
	 * 
	 */

}
